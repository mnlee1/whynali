name: 타임라인 자동 생성 Cron (Production)

on:
  # schedule:
  #   - cron: '*/10 * * * *'  # main 배포 후 활성화
  workflow_dispatch:

jobs:
  auto-timeline:
    runs-on: ubuntu-latest
    steps:
      - name: 타임라인 자동 생성 API 호출 (Production)
        run: |
          curl -X GET https://whynali.vercel.app/api/cron/auto-timeline \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            -H "User-Agent: GitHub-Actions" \
            --max-time 60 \
            --retry 2
