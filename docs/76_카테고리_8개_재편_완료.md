# 카테고리 8개 재편 완료

**작업일**: 2025-03-11
**목적**: 네이버 뉴스 API 기준 8개 카테고리로 재편

## 변경 내역 요약

기존 5개 카테고리 → 8개 카테고리로 확장
- 유지: 사회, 정치, 연예, 스포츠
- 변경: 기술 → IT과학
- 신규: 경제, 생활문화, 세계

## 카테고리 정의

| UI 표시 | 내부값 (DB/코드) | 비고 |
|---------|----------------|------|
| 사회 | 사회 | 유지 |
| 정치 | 정치 | 유지 |
| 연예 | 연예 | 유지 |
| 스포츠 | 스포츠 | 유지 |
| 경제 | 경제 | 신규 |
| IT/과학 | IT과학 | 기존 '기술' 변경 |
| 생활/문화 | 생활문화 | 신규 |
| 세계 | 세계 | 신규 |

내부값(DB, TypeScript)은 슬래시 없이 사용하고, UI 표시만 슬래시 포함

## 파일 변경 내역

### 1. lib/config/categories.ts

**변경 내용**
- CATEGORIES 배열에 경제, IT과학, 생활문화, 세계 추가
- 기존 '기술' → 'IT과학'으로 id 변경, label은 'IT/과학'
- 각 카테고리 키워드, 맥락 규칙, 배지 색상 정의
- CATEGORY_LABELS 자동 생성 (UI 표시용)

**카테고리별 특징**

경제 (신규):
- 색상: yellow 계열
- 키워드: 주가, 코스피, 환율, 금리, 부동산, 아파트, 청약 등
- 맥락 규칙: 주가+급등, 금리+인상, 부동산+가격 등

IT과학 (기존 '기술' 변경):
- 색상: amber 계열 유지
- 키워드: 기존 IT 키워드 + 과학 키워드 추가 (우주, NASA, 로켓 등)
- 맥락 규칙: 기존 규칙 유지 + 우주·NASA 규칙 추가

생활문화 (신규):
- 색상: rose 계열
- 키워드: 맛집, 여행, 패션, 뷰티, 건강, 문화, 전시 등
- 맥락 규칙: 맛집+추천, 여행+추천, 전시+개최 등

세계 (신규):
- 색상: indigo 계열
- 키워드: 미국, 중국, 일본, 러시아, 외교, 국제, 분쟁 등
- 맥락 규칙: 미국+대통령, 중국+외교, 전쟁+분쟁 등

### 2. lib/candidate/category-classifier.ts

**변경 내용**
- AI 프롬프트 카테고리 선택지 5개 → 8개 업데이트
- 각 카테고리 기준 명시
- validCategories 배열 업데이트

**AI 프롬프트 예시**

```
다음 8개 카테고리 중 하나로만 답하라:
- 사회: 국내 사건사고, 사회현상, 범죄, 재난
- 정치: 국회, 정당, 선거, 대통령, 정부 정책
- 연예: 아이돌, 배우, 가수, 드라마, 영화 (국내 연예인)
- 스포츠: 야구, 축구, 농구, 골프, 올림픽 등 스포츠 경기
- 경제: 주식, 부동산, 금리, 무역, 기업 실적, 고용
- IT과학: IT기업, 앱, AI, 반도체, 우주, 과학 연구
- 생활문화: 음식, 여행, 패션, 건강, 교육, 문화 공연
- 세계: 해외 정치, 국제 분쟁, 외교, 해외 사건
```

### 3. supabase/schema.sql

**변경 내용**
- issues 테이블 CHECK 제약 조건 업데이트
- news_data 테이블 CHECK 제약 조건 업데이트
- 마이그레이션 SQL 주석으로 추가

**마이그레이션 SQL**

```sql
-- 1. CHECK 제약 조건 먼저 삭제
ALTER TABLE issues DROP CONSTRAINT IF EXISTS issues_category_check;
ALTER TABLE news_data DROP CONSTRAINT IF EXISTS news_data_category_check;

-- 2. 데이터 변경
UPDATE issues SET category = 'IT과학' WHERE category = '기술';
UPDATE news_data SET category = 'IT과학' WHERE category = '기술';

-- 3. CHECK 제약 조건 재설정
ALTER TABLE issues ADD CONSTRAINT issues_category_check 
  CHECK (category IN ('사회', '정치', '연예', '스포츠', '경제', 'IT과학', '생활문화', '세계'));
ALTER TABLE news_data ADD CONSTRAINT news_data_category_check 
  CHECK (category IN ('사회', '정치', '연예', '스포츠', '경제', 'IT과학', '생활문화', '세계'));
```

Supabase 대시보드 > SQL Editor에서 수동 실행 필요

### 4. lib/config/candidate-thresholds.ts

**변경 내용**
- AUTO_APPROVE_CATEGORIES 기본값 변경
- 기존: '사회,기술,스포츠'
- 변경: '사회,경제,IT과학,생활문화,세계,스포츠'
- 연예/정치는 여전히 수동 승인 필요

### 5. 프론트엔드 페이지

**삭제**
- `app/tech/page.tsx` (기술 페이지)

**신규 생성**
- `app/it-science/page.tsx` (IT/과학)
- `app/economy/page.tsx` (경제)
- `app/lifestyle/page.tsx` (생활/문화)
- `app/world/page.tsx` (세계)

모든 페이지는 동일한 패턴으로 작성됨:

```typescript
import IssueList from '@/components/issues/IssueList'

export default function CategoryPage() {
    return (
        <div className="container mx-auto px-4 py-6 md:py-8">
            <h1 className="text-xl md:text-2xl font-semibold text-neutral-900 mb-6">
                카테고리명 이슈
            </h1>
            <IssueList category="내부값" />
        </div>
    )
}
```

### 6. components/layout/Nav.tsx

**변경 내용**
- CATEGORY_LABELS import 추가
- categories 배열에 8개 카테고리 정의
- URL 경로 업데이트:
    - /tech → /it-science
    - /economy (신규)
    - /lifestyle (신규)
    - /world (신규)
- UI 표시는 CATEGORY_LABELS 사용 (자동으로 'IT/과학', '생활/문화' 표시)

### 7. 환경변수 업데이트

**.env.local**

```bash
AUTO_APPROVE_CATEGORIES=사회,경제,IT과학,생활문화,세계,스포츠
```

## 타입 안정성

categories.ts에서 Union 타입 자동 생성:

```typescript
export type IssueCategory = (typeof CATEGORIES)[number]['id']
// 결과: '사회' | '정치' | '연예' | '스포츠' | '경제' | 'IT과학' | '생활문화' | '세계'
```

이 타입은 types/issue.ts에서 import하여 전체 시스템에서 사용

## UI 표시 라벨 매핑

```typescript
export const CATEGORY_LABELS: Record<string, string> = {
    '사회': '사회',
    '정치': '정치',
    '연예': '연예',
    '스포츠': '스포츠',
    '경제': '경제',
    'IT과학': 'IT/과학',
    '생활문화': '생활/문화',
    '세계': '세계',
}
```

DB와 내부 코드는 슬래시 없이, UI 표시만 슬래시 포함

## 마이그레이션 가이드

### 1. 코드 배포

```bash
# 로컬 테스트
npm run dev

# 빌드 확인
npm run build

# Vercel 배포
git add .
git commit -m "feat: 카테고리 8개 재편 (네이버 API 기준)"
git push origin main
```

### 2. DB 마이그레이션

Supabase 대시보드 > SQL Editor:

```sql
-- 1. 제약 조건 삭제
ALTER TABLE issues DROP CONSTRAINT IF EXISTS issues_category_check;
ALTER TABLE news_data DROP CONSTRAINT IF EXISTS news_data_category_check;

-- 2. 기존 데이터 변경
UPDATE issues SET category = 'IT과학' WHERE category = '기술';
UPDATE news_data SET category = 'IT과학' WHERE category = '기술';

-- 3. 제약 조건 재설정
ALTER TABLE issues ADD CONSTRAINT issues_category_check 
  CHECK (category IN ('사회', '정치', '연예', '스포츠', '경제', 'IT과학', '생활문화', '세계'));
ALTER TABLE news_data ADD CONSTRAINT news_data_category_check 
  CHECK (category IN ('사회', '정치', '연예', '스포츠', '경제', 'IT과학', '생활문화', '세계'));
```

### 3. 배포 순서

1. 먼저 제약 조건 삭제 (Step 1)
2. 코드 배포 (Vercel)
3. 데이터 변경 (Step 2)
4. 제약 조건 재설정 (Step 3)

순서를 지키지 않으면 제약 조건 위반 에러 발생

## 테스트 체크리스트

### 페이지 접근 테스트

- [ ] /it-science (IT/과학 페이지 정상 표시)
- [ ] /economy (경제 페이지 정상 표시)
- [ ] /lifestyle (생활/문화 페이지 정상 표시)
- [ ] /world (세계 페이지 정상 표시)
- [ ] /tech (404 에러 - 삭제됨)

### 네비게이션 테스트

- [ ] 헤더 메뉴에 8개 카테고리 표시
- [ ] 'IT/과학', '생활/문화' 슬래시 표시 확인
- [ ] 클릭 시 해당 페이지 이동
- [ ] Active 상태 스타일 적용

### 카테고리 분류 테스트

- [ ] 경제 관련 뉴스 수집 후 '경제' 분류
- [ ] IT/과학 관련 뉴스 수집 후 'IT과학' 분류
- [ ] 생활/문화 관련 뉴스 수집 후 '생활문화' 분류
- [ ] 세계 관련 뉴스 수집 후 '세계' 분류

### AI 분류 테스트

```bash
# AI 카테고리 분류 활성화 확인
ENABLE_AI_CATEGORY=true
CATEGORY_STRATEGY=ai
```

- [ ] AI가 8개 카테고리 중 하나로 분류
- [ ] 신뢰도 50% 이상이면 채택
- [ ] 신뢰도 낮으면 '사회' 폴백

### 자동 승인 테스트

- [ ] 경제 이슈 화력 30점 이상 자동 승인
- [ ] IT과학 이슈 화력 30점 이상 자동 승인
- [ ] 생활문화 이슈 화력 30점 이상 자동 승인
- [ ] 세계 이슈 화력 30점 이상 자동 승인
- [ ] 연예 이슈는 화력 높아도 수동 승인 대기
- [ ] 정치 이슈는 화력 높아도 수동 승인 대기

## 주의사항

1. **DB 내부값은 항상 슬래시 없이 저장**
    - 올바름: `IT과학`, `생활문화`
    - 틀림: `IT/과학`, `생활/문화`

2. **UI 표시만 CATEGORY_LABELS 사용**
    - 컴포넌트에서 카테고리 표시 시 `CATEGORY_LABELS[category]` 사용

3. **마이그레이션 순서 준수**
    - 제약 조건 삭제 → 코드 배포 → 데이터 변경 → 제약 조건 재설정

4. **기존 '기술' 카테고리 이슈 처리**
    - SQL로 일괄 변경 필수
    - 변경 전 백업 권장

5. **TypeScript 빌드 에러 전체 확인**
    - 타입이 변경되어 기존 하드코딩된 부분에서 에러 발생 가능
    - `npm run build`로 전체 빌드 테스트

## 관련 문서

- `07_이슈등록_화력_정렬_규격.md`: 이슈 등록 규격 (카테고리 정의 포함)
- `99_카테고리_시스템_가이드.md`: 카테고리 시스템 전체 가이드
- `62_AI_전용_모드_적용완료.md`: AI 카테고리 분류

## 다음 작업

1. DB 마이그레이션 실행 (Supabase)
2. 프로덕션 배포 후 모니터링
3. 카테고리별 이슈 분포 확인
4. 필요시 키워드 튜닝

## 변경사항 요약

- ✅ 카테고리 설정 파일 업데이트 (categories.ts)
- ✅ AI 분류기 프롬프트 업데이트 (category-classifier.ts)
- ✅ DB 스키마 업데이트 + 마이그레이션 SQL (schema.sql)
- ✅ 자동 승인 카테고리 확장 (candidate-thresholds.ts)
- ✅ 프론트엔드 페이지 생성/삭제 (4개 신규, 1개 삭제)
- ✅ 네비게이션 메뉴 업데이트 (Nav.tsx)
- ✅ UI 라벨 매핑 자동화 (CATEGORY_LABELS)
- ✅ 타입 안정성 유지 (IssueCategory 자동 생성)
